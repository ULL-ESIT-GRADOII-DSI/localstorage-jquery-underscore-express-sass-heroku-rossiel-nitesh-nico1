
       <table id="result" class="center">
         <% items.forEach(function(name, index) { %>
           <tr class="<%= name.rowClass %>">
             <% name.value.forEach(function(cell,index) { %>
             <%= console.log(cell) %>
               <td><%= cell %></td>
             <% }); %>
           </tr>
         <% }); %>
       </table>
